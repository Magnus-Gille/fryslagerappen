version: 2
updates:
  - package-ecosystem: npm
    directory: /app
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      expo-runtime:
        patterns:
          - expo
          - expo-*
          - "@expo/*"
      test-tooling:
        dependency-type: development
